# Scrap Companion releases

This public repository contains signed alpha release packages for Scrap Companion
testers. It does not contain API keys, invite codes, player data, or the private
release-signing key.

`latest.json` is verified by the Scrap desktop app before any package is accepted.
The actual app remains invite-only through the Scrap Crew service.
